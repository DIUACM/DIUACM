# Copy to `.env.local` to override (gitignored).
#
# Base URL of the API the frontend talks to. Defaults to the production API
# (see src/api/client.ts). Point it at `wrangler dev` for full-local work:
# VITE_API_BASE_URL=http://localhost:8787
