# Copy to `.env.local` to override (gitignored).
#
# The frontend automatically uses http://localhost:8787 during development
# and the deployed API in production. Set this only when you need an override:
# VITE_API_BASE_URL=http://localhost:8788
